<UserControl x:Class="PatioBlox2016.JobPrepUI.Views.KeywordManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="500
             " Background="White">
    <UserControl.Resources>
        <Style x:Key="XferButton" TargetType="Button">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="2 10"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="80" />
            <Setter Property="FontSize" Value="24" />
        </Style>
    </UserControl.Resources>
    <DockPanel>
        <TextBlock DockPanel.Dock="Top"
                   Text="Manage your keywords"
                   FontSize="18" TextAlignment="Center"/>
        <StackPanel DockPanel.Dock="Bottom" 
                    Orientation="Horizontal"
                    MinHeight="36">

        </StackPanel>
        <Grid Margin="9">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="24" />
                <ColumnDefinition />
                <ColumnDefinition Width="24" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <ListBox x:Name="Keywords" />
            <ListBox x:Name="Candidates" Grid.Column="2" />
            <ListBox x:Name="Expansions" Grid.Column="4" />
            
            <StackPanel Grid.Column="1"
                        VerticalAlignment="Center">
                <Button x:Name="MoveToKeywords" 
                        Style="{StaticResource XferButton}"
                        Content="&lt;">
                </Button>
                <Button x:Name="MoveToCandidates"
                        Style="{StaticResource XferButton}"
                        Content="&gt;"/>
            </StackPanel>
        </Grid>
        
    </DockPanel>
</UserControl>
